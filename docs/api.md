# API

> Status: not yet defined. The interface layer (REST/GraphQL/etc.) is decided during
> **ETAP 1 — Analiza architektury** (see `docs/roadmap.md`).

## Index

- Conventions → §1
- Endpoints → §2
- Auth → §3

## 1. Conventions

[Fill in during ETAP 1: protocol, versioning, request/response format, error shape.]

## 2. Endpoints

[Fill in as endpoints are built. One subsection per resource/area once this grows past a
handful of entries — keep the `## Index` above pointing at each.]

## 3. Auth

[Fill in during ETAP 1: authentication/authorization mechanism.]
